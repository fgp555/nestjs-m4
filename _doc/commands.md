```bash
npm install dotenv morgan cors
npm i --save-dev @types/morgan @types/cors

npm i @nestjs/typeorm @nestjs/config typeorm pg mysql2

npm install @nestjs/mapped-types
npm i class-validator class-transformer

# install file uploads

npm i @nestjs/platform-express multer fs-extra
npm i --save-dev @types/multer @types/fs-extra 
```
